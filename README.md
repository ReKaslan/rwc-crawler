# Robtic web crawler (Early release) 

RWC is a web crawler to crawl over the target web server. This tools also have the abilites to interact with elements in the html, but currently this tools is still under development and works on windows only for now.

## Run
Windows: ./rwc.exe <target>